# สุขภาพดี - พอร์ทัลความรู้ด้านสุขภาพ

เว็บแอปพลิเคชันสำหรับจัดเก็บและแบ่งปันแหล่งข้อมูลความรู้ด้านสุขภาพ พร้อมระบบซิงค์ข้อมูลด้วย Supabase

## 🆕 Version 2.0 - Admin Separated!
ระบบผู้ดูแลได้ถูกแยกออกเป็นไฟล์แยกต่างหากเพื่อความปลอดภัยที่ดีขึ้น! 🔒

## 📚 เอกสารประกอบ

### 🔐 ระบบผู้ดูแล (ใหม่!)
- **[ADMIN_GUIDE.md](./ADMIN_GUIDE.md)** - คู่มือระบบผู้ดูแลฉบับสมบูรณ์ 🔑
- **[ENV_SETUP.md](./ENV_SETUP.md)** - การตั้งค่า Environment Variables (.env + Vercel) 🌐

### 🚀 การ Deploy
- **[QUICKSTART.md](./QUICKSTART.md)** - Deploy ใน 5 นาที ⚡
- **[DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)** - Checklist ทีละขั้นตอน ✅
- **[DEPLOYMENT.md](./DEPLOYMENT.md)** - คู่มือแบบละเอียด 📖
- **[ENVIRONMENT_VARIABLES.md](./ENVIRONMENT_VARIABLES.md)** - วิธีตั้งค่าตัวแปร 🔐

### 📖 เอกสารอ้างอิง
- **[PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)** - โครงสร้างโปรเจคและไฟล์ต่าง ๆ
- **ด้านล่าง** - วิธีการรันในเครื่องของคุณเอง (Local Development)

## การติดตั้งและตั้งค่า

### 1. ตั้งค่า Configuration

1. คัดลอกไฟล์ `config.example.js` เป็น `config.js`:
   ```bash
   cp config.example.js config.js
   ```

2. แก้ไขไฟล์ `config.js` และใส่ข้อมูลของคุณ:

```javascript
const CONFIG = {
    // Supabase Configuration
    SUPABASE_URL: 'https://your-project-id.supabase.co',
    SUPABASE_ANON_KEY: 'your-anon-key-here',
    
    // Admin Password
    ADMIN_PASSWORD: 'your-secure-password'
};
```

### 2. หา Supabase URL และ Key

1. เข้าสู่ [Supabase Dashboard](https://supabase.com/dashboard)
2. เลือกโปรเจคของคุณ
3. ไปที่ Settings → API
4. คัดลอก:
   - **Project URL** → ใส่ใน `SUPABASE_URL`
   - **anon/public key** → ใส่ใน `SUPABASE_ANON_KEY`

### 3. สร้างตาราง Database ใน Supabase

รันคำสั่ง SQL นี้ใน Supabase SQL Editor:

```sql
CREATE TABLE health_links (
    id BIGINT PRIMARY KEY,
    title TEXT NOT NULL,
    url TEXT NOT NULL,
    description TEXT,
    category TEXT,
    icon TEXT,
    clicks INTEGER DEFAULT 0,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### 4. เปิดใช้งาน

เปิดไฟล์ `index.html` ในเบราว์เซอร์

## Deploy ไปยัง Vercel 🚀

### วิธีที่ 1: Deploy ผ่าน Vercel Dashboard (แนะนำ)

1. Push โค้ดขึ้น GitHub (ไฟล์ config.js จะไม่ถูก push เพราะอยู่ใน .gitignore)

2. เข้าไปที่ [Vercel Dashboard](https://vercel.com/dashboard)

3. คลิก "Add New Project" และเชื่อมต่อกับ GitHub repository ของคุณ

4. ตั้งค่า Environment Variables ใน Vercel:
   - ไปที่ Settings → Environment Variables
   - เพิ่มตัวแปรเหล่านี้:
   
   ```
   SUPABASE_URL = https://your-project.supabase.co
   SUPABASE_ANON_KEY = your-anon-key-here
   ADMIN_PASSWORD = your-secure-password
   ```

5. คลิก Deploy! 🎉

### วิธีที่ 2: Deploy ผ่าน Vercel CLI

```bash
# ติดตั้ง Vercel CLI
npm install -g vercel

# Login
vercel login

# Deploy
vercel

# ตั้งค่า Environment Variables
vercel env add SUPABASE_URL
vercel env add SUPABASE_ANON_KEY
vercel env add ADMIN_PASSWORD

# Deploy อีกครั้งเพื่อใช้ env variables
vercel --prod
```

### วิธีการทำงาน

เมื่อ deploy บน Vercel:
1. Vercel จะรัน `node generate-config.js`
2. Script จะสร้างไฟล์ `config.js` จาก Environment Variables
3. `index.html` จะโหลด `config.js` ที่สร้างขึ้นมาได้ปกติ

### ตรวจสอบว่า Deploy สำเร็จ

หลังจาก deploy แล้ว เปิดเว็บไซต์และตรวจสอบ:
- ✅ เว็บไซต์โหลดได้ปกติ
- ✅ สามารถเข้าสู่ระบบ Admin ได้
- ✅ ซิงค์ข้อมูลกับ Supabase ได้

## คุณสมบัติ

- ✅ จัดเก็บแหล่งข้อมูลสุขภาพ
- ✅ ค้นหาและกรองตามหมวดหมู่
- ✅ ระบบ Admin สำหรับจัดการข้อมูล
- ✅ ซิงค์ข้อมูลกับ Cloud (Supabase)
- ✅ ทำงานแบบ Offline ด้วย localStorage
- ✅ ติดตามสถิติการเข้าชม

## ความปลอดภัย

⚠️ **สำคัญ:** ไฟล์ `config.js` มีข้อมูลที่ละเอียดอ่อน
- **อย่า** commit ไฟล์ `config.js` เข้า Git
- เปลี่ยนรหัสผ่าน Admin จากค่าเริ่มต้น
- ใช้ Supabase Row Level Security (RLS) เพื่อความปลอดภัยเพิ่มเติม

## การใช้งาน

### สำหรับผู้ใช้ทั่วไป (index.html)
- เรียกดูและค้นหาแหล่งข้อมูลสุขภาพ
- กรองตามหมวดหมู่
- คลิกเพื่อเข้าชมเว็บไซต์ต่างๆ
- ดูสถิติการใช้งาน

### สำหรับผู้ดูแลระบบ (admin.html) 🔐
1. เปิด `admin.html` โดยตรง (เช่น `https://yoursite.com/admin.html`)
2. ใส่รหัสผ่าน (ตามที่ตั้งค่าใน config.js)
3. เพิ่ม/แก้ไข/ลบแหล่งข้อมูล
4. ซิงค์ข้อมูลกับ Cloud
5. ออกจากระบบเมื่อเสร็จ

> **หมายเหตุ:** ไม่มีปุ่มบนหน้าเว็บสำหรับความปลอดภัย ผู้ดูแลต้องรู้ URL โดยตรง

**📖 อ่านคู่มือฉบับเต็มที่:** [ADMIN_GUIDE.md](./ADMIN_GUIDE.md)

## โครงสร้างไฟล์หลัก

```
📁 MENU_Know/
├── 🌐 index.html       # หน้าแรก (สาธารณะ)
├── 🔐 admin.html       # ระบบผู้ดูแล (จำกัดสิทธิ์)
├── 📦 shared.js        # ฟังก์ชันที่ใช้ร่วมกัน
├── ⚙️ config.js        # การตั้งค่า (ไม่ commit)
├── 📋 config.example.js # ตัวอย่างการตั้งค่า
└── 📚 ADMIN_GUIDE.md   # คู่มือผู้ดูแล
```

## License

MIT License

