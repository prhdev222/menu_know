// Legacy no-op build script.
// This project no longer generates/ships config.js to the client.
// Kept only to avoid failing builds in deployments that still run `node generate-config.js`.
console.log('✅ generate-config.js: no-op (client config.js removed; using /api instead)');


