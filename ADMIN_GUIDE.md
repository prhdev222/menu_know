# 🔐 คู่มือระบบผู้ดูแล - สุขภาพดี

## 📋 สารบัญ
- [การแยกไฟล์ระบบ](#การแยกไฟล์ระบบ)
- [วิธีเข้าสู่ระบบผู้ดูแล](#วิธีเข้าสู่ระบบผู้ดูแล)
- [ฟีเจอร์ระบบผู้ดูแล](#ฟีเจอร์ระบบผู้ดูแล)
- [ความปลอดภัย](#ความปลอดภัย)
- [การตั้งค่า](#การตั้งค่า)

---

## 🗂️ การแยกไฟล์ระบบ

ระบบได้ถูกแยกออกเป็น 3 ไฟล์หลักเพื่อความปลอดภัยและจัดการง่ายขึ้น:

### 1. **index.html** (หน้าแรก - สาธารณะ)
- แสดงข้อมูลสุขภาพให้ผู้ใช้ทั่วไป
- ค้นหาและกรองข้อมูล
- ดูสถิติการใช้งาน
- **ไม่มีส่วน admin** - ปลอดภัยกว่า

### 2. **admin.html** (ระบบผู้ดูแล - เข้าถึงจำกัด)
- จัดการข้อมูลสุขภาพ
- เพิ่ม/แก้ไข/ลบแหล่งข้อมูล
- ซิงค์ข้อมูลกับ Cloud
- **ต้องใส่รหัสผ่านก่อนใช้งาน**

### 3. **shared.js** (ฟังก์ชันร่วม)
- การเชื่อมต่อ Supabase
- การจัดการข้อมูล (localStorage + Cloud)
- ฟังก์ชันที่ใช้ร่วมกันทั้ง 2 หน้า

---

## 🔑 วิธีเข้าสู่ระบบผู้ดูแล

### วิธีเดียว: URL โดยตรง (ซ่อนไว้เพื่อความปลอดภัย) 🔒
1. เปิดเบราว์เซอร์
2. พิมพ์: `https://yoursite.com/admin.html`
3. กรอกรหัสผ่านผู้ดูแล

> **หมายเหตุ:** ไม่มีปุ่มบนหน้าเว็บสำหรับเข้าระบบผู้ดูแล เพื่อความปลอดภัย ผู้ดูแลระบบต้องรู้ URL โดยตรงเท่านั้น!

### ตัวอย่าง URL:
```
https://yourdomain.com/admin.html
http://localhost:8080/admin.html
```

---

## 🛠️ ฟีเจอร์ระบบผู้ดูแล

### 1. **เพิ่มแหล่งข้อมูลใหม่**
- ชื่อแหล่งข้อมูล
- URL เว็บไซต์
- คำอธิบาย
- หมวดหมู่ (ทั่วไป, จิตใจ, โภชนาการ, ฟิตเนส, โรค, วิจัย, ฉุกเฉิน)
- ไอคอน Font Awesome (เช่น `fa-heartbeat`)

### 2. **จัดการข้อมูลเดิม**
- ✏️ **แก้ไข** - คลิกปุ่มสีน้ำเงิน
- 🗑️ **ลบ** - คลิกปุ่มสีแดง
- แสดงจำนวนคลิกแต่ละลิงก์

### 3. **ซิงค์ข้อมูล**
- 💾 บันทึกข้อมูลลง localStorage อัตโนมัติ
- ☁️ ซิงค์ไปยัง Supabase Cloud
- แสดงสถานะการซิงค์

### 4. **ออกจากระบบ**
- ลบ session
- กลับไปหน้าแรก

---

## 🔒 ความปลอดภัย

### ✅ การปรับปรุงความปลอดภัย

1. **แยกไฟล์ admin**
   - ผู้ใช้ทั่วไปไม่เห็น code admin
   - ลด attack surface

2. **ไม่มีปุ่ม/ลิงก์จากหน้าหลัก** 🔒
   - ไม่มีปุ่ม "ผู้ดูแลระบบ" บนหน้าเว็บ
   - ผู้ใช้ทั่วไปไม่รู้ว่ามีหน้า admin
   - ต้องรู้ URL โดยตรงเท่านั้น

3. **Session-based Authentication**
   - ใช้ `sessionStorage` เก็บ session
   - หมดอายุเมื่อปิดแท็บ/เบราว์เซอร์

4. **URL ไม่ชัดเจน**
   - ไม่ใช้ `/admin` ที่เดาง่าย
   - สามารถเปลี่ยนชื่อไฟล์ได้ตามต้องการ

### ⚠️ ข้อควรระวัง

1. **รหัสผ่านใน config.js**
   - ควรเปลี่ยนรหัสผ่านเป็นประจำ
   - อย่าใช้รหัสผ่านง่ายๆ

2. **Supabase Keys**
   - เก็บ API Key อย่างปลอดภัย
   - ใช้ Row Level Security (RLS)

3. **HTTPS**
   - ควรใช้ HTTPS เสมอ (Vercel ให้ฟรี)

---

## ⚙️ การตั้งค่า

### 1. เปลี่ยนรหัสผ่าน Admin
แก้ไขไฟล์ `config.js`:
```javascript
const CONFIG = {
    ADMIN_PASSWORD: 'your-new-password', // เปลี่ยนที่นี่
    SUPABASE_URL: '...',
    SUPABASE_ANON_KEY: '...'
};
```

### 2. ตั้งค่า Supabase
1. สร้าง table `health_links` ใน Supabase
2. คัดลอก URL และ Anon Key
3. ใส่ใน `config.js`

### 3. เปลี่ยนชื่อไฟล์ admin (เพิ่มความปลอดภัย)
```bash
# เปลี่ยนชื่อไฟล์
admin.html → system-panel.html

# แก้ไข index.html บรรทัด 58:
<a href="system-panel.html" class="...">
```

### 4. Deploy
```bash
# Vercel
vercel --prod

# Netlify
netlify deploy --prod
```

---

## 📊 โครงสร้างไฟล์

```
MENU_Know/
├── index.html          # หน้าแรก (สาธารณะ)
├── admin.html          # ระบบผู้ดูแล (จำกัดสิทธิ์)
├── shared.js           # ฟังก์ชันร่วม
├── config.js           # การตั้งค่า
├── config.example.js   # ตัวอย่างการตั้งค่า
└── ADMIN_GUIDE.md      # คู่มือนี้
```

---

## 🚀 ขั้นตอนการใช้งานครั้งแรก

1. ✅ ตั้งค่า `config.js` (รหัสผ่าน, Supabase)
2. ✅ เปิด `index.html` ตรวจสอบหน้าแรก
3. ✅ คลิก "ผู้ดูแลระบบ" หรือเปิด `admin.html`
4. ✅ ใส่รหัสผ่านเข้าสู่ระบบ
5. ✅ เพิ่ม/แก้ไขข้อมูลสุขภาพ
6. ✅ คลิก "ซิงค์ข้อมูล" เพื่อบันทึกลง Cloud
7. ✅ ออกจากระบบเมื่อเสร็จ

---

## 💡 เคล็ดลับ

### เพิ่มความปลอดภัย
- เปลี่ยนชื่อไฟล์ `admin.html` เป็นชื่ออื่น
- ใช้รหัสผ่านที่แข็งแรง (8+ ตัวอักษร, ผสมตัวเลขและสัญลักษณ์)
- ตั้งค่า RLS ใน Supabase

### การใช้งาน
- ซิงค์ข้อมูลเป็นประจำ
- สำรองข้อมูลด้วย Export/Import
- ตรวจสอบสถิติการคลิกเป็นประจำ

---

## 🆘 แก้ปัญหา

### ปัญหา: ไม่สามารถเข้าสู่ระบบ
- ตรวจสอบรหัสผ่านใน `config.js`
- ลบ cache เบราว์เซอร์
- ลองใช้ Incognito Mode

### ปัญหา: ซิงค์ไม่ได้
- ตรวจสอบ Supabase URL และ Key
- ดู Console เพื่อดู error
- ตรวจสอบ internet connection

### ปัญหา: ข้อมูลหาย
- ตรวจสอบ localStorage (F12 → Application → Local Storage)
- ดึงข้อมูลจาก Supabase
- Restore จาก backup

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย:
- เปิด Issue ใน GitHub
- อ่านเอกสารเพิ่มเติมใน `README.md`

---

**สร้างโดย:** ระบบสุขภาพดี  
**อัปเดตล่าสุด:** 2025  
**เวอร์ชัน:** 2.0 (Admin Separated)

